import { useState,useEffect } from 'react';import API from './api';export default function App(){const[tasks,setTasks]=useState([]);const[form,setForm]=useState({title:'',importance:'medium',deadline:''});const load=async()=>{setTasks((await API.get('/tasks')).data)};const create=async()=>{await API.post('/tasks',form);load()};useEffect(()=>{load()},[]);return(<div style={{padding:20}}><h1>Simple Task Manager</h1><input placeholder='Title' onChange={e=>setForm({...form,title:e.target.value})}/><select onChange={e=>setForm({...form,importance:e.target.value})}><option>low</option><option>medium</option><option>high</option></select><input type='date' onChange={e=>setForm({...form,deadline:e.target.value})}/><button onClick={create}>Add</button><div style={{marginTop:20}}><h3>Tasks</h3>{tasks.map(t=><div key={t._id} style={{border:'1px solid #ccc',padding:5,margin:5}}><b>{t.title}</b><br/>Importance: {t.importance}<br/>Deadline: {t.deadline?.substring(0,10)}<br/>Priority: {t.priority}</div>)}</div></div>) }